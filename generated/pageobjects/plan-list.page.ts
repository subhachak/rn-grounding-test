// GENERATED by generator/cli.mts from PlanListScreen. Do not edit; change the
// app's testIDs and regenerate.
import Page from './base.page';

class PlanListPage extends Page {
  // testID=plan-list-screen (src/screens/PlanListScreen.tsx:16)
  get root() {
    return this.byTestId("plan-list-screen");
  }

  // testID={`plan-item-${item.id}`} (src/screens/PlanListScreen.tsx:22)
  planItem(id: string) {
    return this.byTestId(`plan-item-${id}`);
  }

  // testID={`plan-item-label-${item.id}`} (src/screens/PlanListScreen.tsx:25)
  planItemLabel(id: string) {
    return this.byTestId(`plan-item-label-${id}`);
  }

  // testID={`plan-item-entitled-badge-${item.id}`} (src/screens/PlanListScreen.tsx:27)
  // renders only when item.entitled
  planItemEntitledBadge(id: string) {
    return this.byTestId(`plan-item-entitled-badge-${id}`);
  }

  // testID={`plan-item-restricted-badge-${item.id}`} (src/screens/PlanListScreen.tsx:29)
  // renders only when !(item.entitled)
  planItemRestrictedBadge(id: string) {
    return this.byTestId(`plan-item-restricted-badge-${id}`);
  }
}

export default new PlanListPage();
